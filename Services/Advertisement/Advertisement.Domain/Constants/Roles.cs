namespace Advertisement.Domain.Constants;

public static class Roles
{
    public const string User = nameof(User);
    public const string Admin = nameof(Admin);
    public const string Moderator = nameof(Moderator);
}
