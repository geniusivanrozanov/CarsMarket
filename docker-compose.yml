version: "3.9"

services:
  identity.api:
    build:
      context: .
      dockerfile: Services/IdentityService/IdentityService.WebAPI/Dockerfile
  
  postgres:
    image: postgres:latest
    
  redis:
    image: redis:latest
    
volumes:
  postgres_data:
  redis_data: